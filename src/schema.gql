# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type search {
  id: ID!
  title: String!
  description: String!
}

type Query {
  search(query: String!, filters: SearchFilters!, pagination: PaginationInput!): search
  getSearchResult(id: Float!): search
  getUserSearchHistory(userId: Float!): search
  getPopularSearch: search
}

input SearchFilters {
  name: Float!
}

input PaginationInput {
  name: Float!
}

type Mutation {
  createUserSearchHistory(userId: Float!, query: String!): search
  clearUserSearchHistory(userId: Float!): search
  createSearchResult(title: String!, url: String!, description: String!): search
  updateSearchResult(input: UpdateSearchResult!): search
  deleteSearchResult(id: Float!): search
}

input UpdateSearchResult {
  id: Float!
}